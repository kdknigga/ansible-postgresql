# This file was originally managed by linux-system-roles/.github but has been
# taken over manually to preserve the community.general version lower bound.
#
# IMPORTANT: The community.general >= 8.2.0 lower bound is required because
# tasks/repos_extensions.yml uses community.general.dnf_config_manager, which
# was added in community.general 8.2.0. Do NOT remove or relax this constraint.
# If upstream CI tooling regenerates this file, re-add the version constraint
# before merging.
---
collections:
  - name: ansible.posix
  - name: fedora.linux_system_roles
  - name: community.general
    version: '>=8.2.0,<12.0.0'
